// Notification Module (BE-7, FR-11): queue consumer, email/push адаптеры,
// per-type preferences (контракт web/types/notification.ts)
import { stubRouter } from '../stub-router.js';

export const notificationRouter = stubRouter('notification');
