// Listing Module (BE-3, FR-02/03/05, §6.2): draft CRUD, photos upload → ML blur,
// price-estimate, publish → async fraud check, статусная машина 07 §2.1
import { stubRouter } from '../stub-router.js';

export const listingRouter = stubRouter('listing');
